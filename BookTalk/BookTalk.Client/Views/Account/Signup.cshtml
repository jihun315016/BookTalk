@{
    Layout = "_AccountLayout";
}

@using BookTalk.Shared.ViewModels

@model UserViewModel?

@using (Html.BeginForm("Signup", "Account", FormMethod.Post, new { @class = "form-container custom-base-container px-4 py-3" }))
{
    <h5 class="text-center">Sign up</h5>

    <div class="form-group">
        @Html.LabelFor(m => m.InputId, new { @class = "control-label", @for = "exampleDropdownFormID" })
        @Html.TextBoxFor(m => m.InputId, new { @class = "form-control", @id = "exampleDropdownFormID", @placeholder = "Your ID" })
        @Html.ValidationMessageFor(m => m.InputId, "", new { @class = "text-danger" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.InputPassword, new { @class = "control-label", @for = "exampleDropdownFormPassword1" })
        @Html.PasswordFor(m => m.InputPassword, new { @class = "form-control", @id = "exampleDropdownFormPassword1", @placeholder = "Password" })
        @Html.ValidationMessageFor(m => m.InputPassword, "", new { @class = "text-danger" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.InputName, new { @class = "control-label", @for = "exampleDropdownFormName" })
        @Html.TextBoxFor(m => m.InputName, new { @class = "form-control", @id = "exampleDropdownFormName", @placeholder = "Your name" })
        @Html.ValidationMessageFor(m => m.InputName, "", new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-primary float-right">Sign up</button>
}

<script>
    window.onload = function () {
        OnLoadView();
    }

    function OnLoadView() {
        var sErrorMessage = "@Html.Raw(ViewBag.ErrorMessage)";

        if (sErrorMessage) {
            alert(sErrorMessage);
        }
    }
</script>

